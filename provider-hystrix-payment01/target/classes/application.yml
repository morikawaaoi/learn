server:
  port: 8001
spring:
  application:
    name: provider-hystrix-payment-service
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      #集群模式 指向其他eureka
      #defaultZone: http://eureka7002.com:7002/eureka/
      #单机模式 指向自己
      defaultZone: http://eureka7001.com:7001/eureka/
